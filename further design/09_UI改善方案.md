# 09 · UI 改善方案（风格一体化 + 多槽读档）

> 目的：① 让 UI 外壳（对话框、窗口、按钮、标题页）与游戏画面素材"长在一起"——
> 现在画面是像素国风，UI 却是扁平蓝灰 CSS，两种画风；② 读档改为多档位 +
> 二次确认，防止误触毁档/误读档。
>
> **本文档仅为设计稿，尚未接入游戏。** 主程序 agent 按"所需改动"列施工。

## 一、现状诊断

### 素材现状（已 review，共 366 个文件）

| 类别 | 路径 | 规格 | 画风 |
|------|------|------|------|
| 对话头像 | `assets/chars/face/*.png`（34 个） | 64×64 | 像素国风胸像 |
| Boss 战斗立绘 | `assets/chars/boss/*.png`（40 个，含夏侯惇/司马懿多形态） | 64×64 | 像素国风全身 |
| 地图小人 | `assets/chars/map/*.png`（90+ 个） | 64×64 | 同上 |
| 地形/过渡 | `assets/gfx/v4/Terrain/**`（60+ 个，草地/道路/水面/山地/洞穴/农田+边缘过渡） | tile | 像素 tileset |
| 建筑 | `assets/gfx/v4/Buildings/**`（民居/店铺带招牌/城门/宫殿） | 2×2、4×2 tile | 同上 |
| 道具摆件 | `assets/gfx/v4/Props/**`（宝箱/水井/篝火/宫殿龙椅等） | tile | 同上 |

**画面侧画风已统一**（像素 + 国风题材，`image-rendering: pixelated` 渲染）。

### 问题：UI 外壳是另一套画风

`index.html` 里全部 UI 都是扁平 CSS：深蓝灰面板 `#141c30`、圆角 10~14px、
亮蓝描边 `#4f8cff`、按钮 `#24304f` 纯色块。和像素画面拼在一起，像"网页弹窗
贴在游戏上"。涉及处：对话框 `#dialog`、通用面板 `#panel`、战斗日志/指令框
`#battle-log/#battle-cmd`、toast、标题页 `#scr-title`、十字键/AB 键 `#dpad`、
章节插画边框 `#illust`。

### 存档现状

- 单档位：`localStorage["taoyuan3g_save"]`，`engine/save.js` 的
  `saveGame()/loadGame()` 无槽位概念（存档体内已有 `ts` 时间戳，可直接用于档位展示）。
- 标题页 `继续征途（读档）`（`engine/core.js:295`）点击**直读**，无确认；
- 游戏内菜单 `存档/读档`（`engine/ui.js:546-555`）同样直存直读，无确认、无槽位。

## 二、需要用户提供的素材（UI Kit）

**统一规格**：像素国风，与 `gfx/v4` 相同颗粒度；木纹/竹简/宣纸色系 + 描金或
深棕描边；PNG 透明底；全部做成**九宫格切片**（四边可拉伸、四角固定），
CSS 用 `border-image` 或 canvas 绘制。优先级：必需 = 一体化最低闭环。

| 选 | 优先级 | 文件（建议名） | 规格 | 用途 |
|----|--------|----------------|------|------|
| ☐ | 必需 | `assets/ui/panel_9slice.png` | 48×48（边 16px） | 所有弹窗/面板的框体：通用面板、战斗指令框、确认弹窗、档位选择层 |
| ☐ | 必需 | `assets/ui/dialog_9slice.png` | 48×48（边 16px，底部留▼位置） | 对话框底框（替代 `#dialog` 纯 CSS） |
| ☐ | 必需 | `assets/ui/btn_9slice.png`（三态合一或三张） | 48×16（边 8px；normal / hover / pressed） | 全部按钮：菜单、战斗指令、标题页、确认弹窗 |
| ☐ | 必需 | `assets/ui/title_bg.png` | 480×270 或 960×540 | 标题页背景（桃园/山水/烽烟题材，呼应"桃园三国"） |
| ☐ | 推荐 | `assets/ui/sel_arrow.png` | 16×16（▶ 与 ▼ 两枚） | 菜单光标、对话继续指示 |
| ☐ | 推荐 | `assets/ui/icons.png` | 16×16 ×N 一排 | 菜单图标：卷轴(存档)、剑(出战)、钱袋(金币)、衣甲(装备)等 |
| ☐ | 可选 | `assets/ui/dpad_9slice.png` + `assets/ui/ab_btn.png` | 十字键 52×52、AB 圆键 58×58 | 触屏按键统一画风 |
| ☐ | 可选 | `assets/ui/divider.png` | 48×8 横向云纹/回纹 | 面板内分隔线、标题下装饰 |

**若素材暂缺**（降级方案，先写进方案备着）：
- 用 CSS 模拟——深棕底 `#2a1f14` + 双层描边（外 `#6a4a2a` / 内 `#c8a03a`）+
  直角（去掉圆角）+ `image-rendering: pixelated` 风格的硬阴影，可做到
  "不惊艳但统一"；
- 标题页背景可临时用现有章节插画 `assets/illust/chXX.png` 平铺/叠暗色渐变。

## 三、读档多档位 + 确认弹窗（核心需求，P0，不依赖素材）

### 交互流程

```
标题页 / 游戏内菜单
  └─ 点击「继续征途（读档）」
       └─【档位选择层】三个档位卡片（纵向列表）
            ├─ 档位一  第六章 · 新野城  刘备 Lv26  07-26 21:03
            ├─ 档位二  第四章 · 白马坡  关羽 Lv18  07-24 20:41
            └─ 档位三  —— 空档位 ——（置灰不可选）
                 └─ 点击有效档位
                      └─【确认弹窗】（复用通用确认组件）
                           "读取此档？
                            将回到：第六章 · 新野城（刘备 Lv26）
                            当前未保存的进度将丢失。"
                           [ 确认读取 ]  [ 取消 ]
                            └─ 确认 → loadGame(slot) → 进地图
```

- **空档位**：读档流程置灰不可点；存档流程可选（写入新档）。
- **游戏内菜单的「存档」对称改造**：点开同样是三档选择 → 点已占用档位时确认弹窗
  文案改为 `"该档位已有记录（第X章 · 地图名），覆盖？"` [确认覆盖][取消]；
  点空档位直接写入并 toast 成功（新档无损失，无需确认）。
- **取消（B 键 / 点击遮罩）逐层返回**：确认弹窗 → 档位层 → 标题页/菜单。
- 键盘：↑↓ 移动光标（用 `sel_arrow`），Enter/Z 确认，Esc/X 返回——与现有
  操作习惯一致（`index.html` 标题页 hint 文案同步更新）。

### 数据层改动（`engine/save.js`，全部向后兼容）

- 存档键：`taoyuan3g_save_1` / `_2` / `_3`；**旧单档 `taoyuan3g_save` 自动迁入
  档位一后删除旧键**（老玩家进度不丢）。
- `saveGame(slot)` / `loadGame(slot)` / 新增 `listSlots()`：返回三槽元信息
  `{ slot, empty, chapter, map, heroName, heroLv, ts }`（读档时只解析头部字段，
  不做 `applySave`）。
- 档位卡片展示：`第X章 · 地图名 · 队长名 Lv · 存档时间`——所需字段
  `chapter/map/ts` 存档体内已有，`party[0]` 取队长，零新增字段。
- `migrateSave` 逻辑不变，逐槽独立迁移。

### UI 组件改动

- 新增**通用确认弹窗组件** `confirmBox({ title, lines, okText, cancelText, onOk })`
  ——基于现有 `#panel` 实现，z-index 压一层；读档、覆盖存档复用同一个，
  后续删档、 shop 大额购买也能用。
- 档位选择层同样基于 `#panel` 扩展（列表项 = 档位卡片按钮），不写死读档专用。

## 四、UI 一体化改造清单（应用 UI Kit，P1）

| 选 | 部位 | 现状 | 改造 |
|----|------|------|------|
| ☐ | 对话框 `#dialog` | 蓝灰圆角 CSS | `dialog_9slice` 底框；头像框换木质描边；▼ 换 `sel_arrow` |
| ☐ | 通用面板 `#panel` | 蓝灰圆角 | `panel_9slice`；标题描金字 |
| ☐ | 战斗 `#battle-log` / `#battle-cmd` | 蓝灰/暗红边 | log 用宣纸色底深字、指令框用 `panel_9slice`；按钮换三态图 |
| ☐ | 全部按钮 | 纯色块 | `btn_9slice` 三态；ghost 次要按钮降级为深色态 |
| ☐ | 标题页 `#scr-title` | 纯黑底+两行按钮 | `title_bg` 全屏背景 + 标题描金大字 + 按钮 UI kit |
| ☐ | toast | 蓝边弹条 | 小号 `panel_9slice` 样式 |
| ☐ | 十字键/AB 键 `#dpad` | 半透明圆角块 | `dpad_9slice` / `ab_btn`（可选素材，缺则沿用现样式） |
| ☐ | 章节插画 `#illust` | 蓝边框 | 换木质/描金边框（可复用 panel 切片） |

**落法**：新增 `assets/ui/` 目录 + `data/ui_assets.js` 注册表（参照
`data/portraits.js` 的注册方式）；CSS 统一改为 `border-image` 引用切片；
素材缺失时自动回退第二节的降级 CSS 方案——**保证素材没给齐也能跑**。

## 五、分批实施建议

- **P0（纯代码，立即可做）**：三槽存档 + 档位选择层 + 确认弹窗组件 +
  旧档迁移。不依赖任何素材。
- **P1（依赖必需素材 4 件）**：对话框 / 面板 / 按钮 / 标题页背景四处换皮，
  UI 立刻"长在一起"。
- **P2（依赖推荐/可选素材）**：光标、图标、十字键、分隔线等装饰层，
  可给可不给，给了就贴。

> 每条独立可勾选；素材到一件贴一件，回退方案兜底。

## 六、给 Opus 的画图提示词（逐件可直接复制）

> 使用方式：把下面【全局风格串】拼在每条提示词**开头**一起发给 Opus；
> 同一批素材尽量在同一次会话里生成，并把已生成的成品作为风格参考图
> 回喂给它（"保持与这张一致的调色板和笔触"）。
>
> 英文风格锚点（Opus 转调图像模型时可带上）：
> `16-bit pixel art, Chinese Three Kingdoms RPG UI, dark lacquered wood + rice paper + gold trim, hard pixel edges, limited palette, no anti-aliasing, transparent background, no text, no watermark`

【全局风格串】（每条提示词前都拼上这段）：

```
复古像素风（pixel art），16-bit 中式三国 RPG 的 UI 组件，与游戏内像素
tileset 同一画风。统一调色板：深漆木 #3a2a1a、原木 #6a4a2a、描金 #c8a03a、
宣纸 #e8d8b0、墨色 #1a1410。硬边像素、有限色数、不要抗锯齿模糊、不要渐变
光效、不要文字和水印。PNG 透明背景。
```

### 1. panel_9slice.png（必需 · 48×48）

```
【全局风格串】
画一个 48×48 像素的弹窗面板底框，用于九宫格（9-slice）拉伸：
- 四个角各 16×16：固定的回纹木雕角花，描金勾边；
- 四条边各 16px 宽：可无缝平铺的竖直/水平木纹边框；
- 中心区域：深色宣纸底（#2a1f14 偏墨色），可拉伸；
- 整体像一个中式木框装裱的深色卷轴面板。
请精确按 16px 网格对齐，角与边必须能在 16px 处切开无缝拼接。
```

### 2. dialog_9slice.png（必需 · 48×48）

```
【全局风格串】
画一个 48×48 像素的对话框底框，九宫格（9-slice）规格同上（16px 边），
但有两点不同：
- 底边中央留一个朝下的三角缺口/小旗形装饰（对话指示箭头的挂点）；
- 底色比面板略浅（宣纸 #e8d8b0 的暗化版），便于深色文字阅读。
其余与 panel_9slice 保持完全一致的木纹、角花和调色板。
```

### 3. btn_9slice.png（必需 · 48×16 ×3 三态）

```
【全局风格串】
画一组木牌按钮，三个状态横向排列在一张 144×16 的图上（每张 48×16，
九宫格边 8px，可横向拉伸）：
1) normal：原木色牌面，深棕描边；
2) hover：牌面提亮一档，描边变描金；
3) pressed：牌面整体压暗下陷，描边内缩 1px 阴影。
三张必须严格同形、同尺寸、同网格，仅明度和描边不同。
```

### 4. title_bg.png（必需 · 960×540）

```
【全局风格串】
画一幅 960×540 像素的游戏标题页背景：黄昏的桃园——前景几株盛开的
桃花树，中景一座茅亭与石案（暗示桃园结义），远景层叠青山与烽烟，
天空暖橙色。像素山水画，细节集中在四周；画面中央偏下 1/3 区域保持
空旷、压暗，留给游戏标题大字和按钮。不要出现人物特写和任何文字。
```

### 5. sel_arrow.png（推荐 · 16×16 两枚）

```
【全局风格串】
画两枚 16×16 像素的菜单光标，横排在一张 32×16 的图上：
1) 朝右小箭头 ▶（菜单选项光标）；2) 朝下小箭头 ▼（对话继续指示）。
描金实心像素箭头，带 1px 深色描边，造型略带毛笔尖的弧度。
```

### 6. icons.png（推荐 · 16×16 ×6 一排）

```
【全局风格串】
画一排 6 枚 16×16 像素菜单图标，横排在一张 96×16 的图上，等距对齐：
1) 卷轴（存档）2) 打开的书卷（读档）3) 青铜剑（战斗）
4) 钱袋（金钱）5) 衣甲（装备）6) 酒葫芦（道具）。
统一描金+深棕描边的小图标风格，剪影清晰，一眼可辨。
```

### 7. dpad + AB 键（可选 · 52×52 与 58×58）

```
【全局风格串】
画触屏按键贴图两张：
1) 52×52 十字键单键：圆角方形木牌，中央一枚深色方向三角；
2) 58×58 圆形按键 A/B 各一枚：圆形木牌镶金边，A 键牌面偏青绿
（#3a6e3c 像素化）、B 键偏赭红（#6e3a3a 像素化），中央留空
（字母由程序绘制，不要画在图里）。
```

### 8. divider.png（可选 · 48×8）

```
【全局风格串】
画一条 48×8 像素、可横向无缝平铺的装饰分隔条：中式回纹/云纹，
描金线条配深棕底，左右两端必须能无缝衔接。
```

### 验收与后处理（Opus 交图后核对）

- 尺寸必须精确（48×48、960×540 等）；如需缩放，用**最近邻**（nearest
  neighbor），禁双线性——像素画一糊就毁。
- 九宫格件铺在 16px/8px 网格上检查：边缝无错位、四角对称。
- 把成品与 `assets/gfx/v4/` 任意建筑图并排放一眼：调色板不违和即通过。
- 背景必须真透明（alpha=0），不是白色或近白色。

## 七、游戏进入页 + 章节插画提示词（逐张可复制）

> 现状：`assets/illust/ch00~ch11.png + end.png` 共 13 张，全是
> `tools/gen_art.py` 程序化生成的占位图（480×270，三角形火苗级别），
> 与新的角色/场景素材画风完全脱节。以下每张给出关键词和完整提示词，
> **同名替换**即可（引擎 `engine/map.js:81` 按 `chXX.png` 引用，零改代码）。
>
> 统一规格：**480×270 横版**，像素国风，硬边像素，**不含任何文字**
> （章节名由程序叠加在图下方）。全局风格串同第六节，每条开头照拼。

### 0. 游戏进入页（开屏主视觉）

> 与第六节的 `title_bg` 分工：`title_bg` 是标题页底图（中央留按钮位）；
> 本张是进入游戏前的**开屏主视觉**（splash，满屏插画 + "点击任意处进入"）。
> 若想省一件，可只画本张，标题页直接复用它加暗色渐变。

```
【全局风格串】
画一幅 960×540 像素的游戏开屏主视觉：桃园结义——三株盛开的桃花树下，
刘关张三人背影立于石案前举杯（背影或侧影，不要脸部特写），桃花花瓣
飘落，远处晨光初升。像素画，暖粉与金色晨光为主调，构图饱满、居中，
画面底部 1/5 压暗留给"点击任意处进入"提示。不要任何文字。
```

### 1. ch00 · 序章「徐州剿匪」——关键词：桃园、结义军、初战、晨光

```
【全局风格串】
480×270 像素插画：桃园边初战告捷——桃花林中，三骑并辔立于前景，
身后乡勇结阵，远处黄巾残部抱头溃逃，晨光从桃林间隙洒落。
暖色调，明快昂扬。不要文字。
```

### 2. ch01 · 第一章「父仇之火」——关键词：徐州城、战火、曹军压境

```
【全局风格串】
480×270 像素插画：徐州保卫战——徐州城楼耸立，城头守军张弓，
城下曹军黑压压一片压境，营火连绵，天边战火映红。
冷峻紧张，以铁灰与火红对比。不要文字。
```

### 3. ch02 · 第二章「三让徐州」——关键词：辕门射戟、小沛、方天画戟

```
【全局风格串】
480×270 像素插画：辕门射戟——军营辕门外，吕布张弓立射，
一支箭正中远处辕门上方天画戟的小枝，两旁纪灵与刘备军将士
仰头惊望。戏剧性瞬间，人物剪影夸张。不要文字。
```

### 4. ch03 · 第三章「寄人篱下」——关键词：青梅煮酒、雷夜、论英雄

```
【全局风格串】
480×270 像素插画：青梅煮酒论英雄——许都相府后园，曹操与刘备
对坐石案煮酒，案上青梅与酒樽，夜空一道惊雷劈落，二人神情
各异地对望。暗色调，雷光瞬间照亮庭院。不要文字。
```

### 5. ch04 · 第四章「风云再散」——关键词：白马坡、万军斩将、青龙刀

```
【全局风格串】
480×270 像素插画：白马坡斩颜良——千军万马的军阵之中，一骑
红脸长髯的猛将策马突前，青龙偃月刀高举劈落，敌军旗帜纷纷
倒伏让路，尘土飞扬。动势极强的冲锋构图。不要文字。
```

### 6. ch05 · 第五章「千里走单骑」——关键词：单骑、关门、黄河渡、黄昏

```
【全局风格串】
480×270 像素插画：千里走单骑——黄昏古道上，关羽单骑横刀立马，
身后是刚闯过的巍峨关门，前方黄河渡口波光粼粼，赤兔马扬鬃，
一行归雁掠过长天。苍凉壮阔，暖橙色剪影。不要文字。
```

### 7. ch06 · 第六章「卧龙出山」——关键词：隆中、草庐、三顾、竹林雪

```
【全局风格串】
480×270 像素插画：三顾茅庐——隆中竹林深处一间草庐，庐前
一位手持羽扇的布衣先生（诸葛亮）缓步而出，门外刘关张三人
躬身长揖，竹梢覆着薄雪，一只仙鹤掠过。清冷空气感，青白主调。
不要文字。
```

### 8. ch07 · 第七章「火烧博望」——关键词：博望坡、峡谷、火海、秋草

```
【全局风格串】
480×270 像素插画：火烧博望坡——狭长山谷两侧烈焰冲天，
谷中曹军溃乱奔逃、旗帜倾倒，火光映红夜空，山崖上蜀军
擂鼓俯冲。以大面积橙红火光为主，画面要有"火墙"的压迫感。
（替换现在那张三角形火苗占位图。）不要文字。
```

### 9. ch08 · 第八章「赤壁鏖兵」——关键词：赤壁、火船、连环、东风

```
【全局风格串】
480×270 像素插画：火烧赤壁——长江夜战，江面上铁索相连的曹军
楼船陷入一片火海，火船顺东风冲入水寨，烈焰映红半壁江天，
远处周郎战船列阵逼近。红黑对比，江面倒影火光粼粼。不要文字。
```

### 10. ch09 · 第九章「荆南四郡」——关键词：长沙、老将对决、城下

```
【全局风格串】
480×270 像素插画：战长沙——长沙城下，白发老将黄忠立马弯弓，
与横刀的关羽遥遥相对，城楼高耸，两军屏息观战，夕阳把两道
身影拉得很长。英雄相惜的张力，暖金色。不要文字。
```

### 11. ch10 · 第十章「西川风云」——关键词：定军山、山巅、老将军、鹿角

```
【全局风格串】
480×270 像素插画：定军山——高山之巅，老将黄忠立马俯瞰，
山下曹军鹿角营寨一览无余，一军正从山道俯冲而下如猛虎出柙，
山风卷动战旗。居高临下的构图，山势险峻。不要文字。
```

### 12. ch11 · 终章「出师未捷」——关键词：五丈原、秋风、将星、营帐

```
【全局风格串】
480×270 像素插画：五丈原秋风——暮色下的蜀军连营，中军帐前
一盏孤灯，白发丞相凭案远眺（背影），秋原衰草连天，天边
一颗将星忽明忽暗。萧索苍茫，冷灰与孤灯暖黄对比。不要文字。
```

### 13. end.png · 谢幕「星落五丈原」——关键词：流星、灯灭、山河

```
【全局风格串】
480×270 像素插画：星落五丈原（谢幕图）——辽阔夜空，一颗流星
划过五丈原上空坠向远山，山下营帐灯火渐次熄灭，唯余秋风
卷着落叶。全图最冷最静的一张，深蓝夜幕为主，只留一星余光。
不要文字。
```

### 验收（本节专属）

- 全部 480×270、同名替换 `assets/illust/`，引擎零改动；
- 13 张同批生成，调色板与画风互相参照（每出一张回喂作参考）；
- 章节插画在引擎里以 `min(92vw, 480px)` 显示 + `pixelated` 渲染，
  480×270 正好 1:1，**不要画成 960×540**（非整数缩放会花）；
- 图中一律不出现文字——章节标题由程序用 `#illust-cap` 叠加。
